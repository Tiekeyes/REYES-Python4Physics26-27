import numpy as np

#Scalar Arithmetic
array = np.array([1, 2, 3])

#add one to each element
array += 1

#raise each element to the power of 5
array = array**5


new_array = np.array([4, 9, 16])
print(lambda x: int(x) (np.sqrt(new_array)))